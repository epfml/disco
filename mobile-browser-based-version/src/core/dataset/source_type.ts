/* eslint-disable no-unused-vars */
export enum SourceType {
    // For a dataset comprised of a matrix of samples X and a vector of labels y...
    // ... represents the samples X
    SAMPLES = 'Samples',
    // ... represents the labels y
    LABELS = 'Labels',
    // ... represents the dataset {X, y}
    DATASET = 'Dataset'
}
