{
  "name": "onnx-converter",
  "private": true,
  "type": "module",
  "main": "dist/gpt2_from_onnx.js",
  "scripts": {
    "convert_onnx": "pnpm run build && node dist/convert_onnx.js",
    "watch": "nodemon --ext ts --ignore dist --exec pnpm run",
    "build": "tsc --build && cp -r src/protobuf dist",
    "lint": "pnpm exec eslint .",
    "test": ": nothing"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@epfml/discojs": "workspace:",
    "@tensorflow/tfjs-node": "catalog:",
    "immutable": "catalog:",
    "long": "4.0.0",
    "protobufjs": "7.6.3"
  },
  "devDependencies": {
    "nodemon": "3.1.14"
  }
}
